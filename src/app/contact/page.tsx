import { ContactContent } from "./contact-content";

export const metadata = {
  title: "Contact Us",
  description: "Get in touch with Goodie Box Store — we'd love to hear from you.",
};

export default function ContactPage() {
  return <ContactContent />;
}
